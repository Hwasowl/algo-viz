export const examTopic = {
  id: 'exam',
  week: 5,
  title: '🏆 기출 유형 (카카오·삼성·라인)',
  plainDef: '실제 코테에 가장 자주 나오는 4가지 유형과, 각 유형을 Java로 어떻게 접근하는지. 카카오·삼성·라인 기출의 80% 이상이 중~중하 난이도다.',
  article: [
    {
      callout: {
        tone: 'info',
        icon: '📌',
        title: '먼저 알아둘 것',
        body: '대부분의 코테는 어려운 알고리즘 지식보다 "문제를 정확히 읽고, 시키는 대로 빠짐없이 구현"하는 힘을 본다. 화려한 알고리즘보다 구현·완전탐색이 더 자주 나온다.',
      },
    },

    { h: '유형 1 · 구현 / 시뮬레이션 (가장 많음)' },
    {
      p: '문제에 적힌 규칙을 그대로 코드로 옮기는 유형. 어려운 아이디어는 없지만 조건이 많아 꼼꼼함이 핵심이다. 격자(2차원 배열) 위 이동이 단골.',
      terms: [
        { word: '시뮬레이션', desc: '문제의 규칙·과정을 한 단계씩 그대로 흉내 내어 결과를 구하는 유형.' },
      ],
    },
    {
      code: `// 격자 4방향 이동의 정석 — 방향 배열 + 경계 체크
int[] dx = {-1, 1, 0, 0};
int[] dy = {0, 0, -1, 1};
for (int d = 0; d < 4; d++) {
    int nx = x + dx[d], ny = y + dy[d];
    if (nx < 0 || nx >= N || ny < 0 || ny >= M) continue; // 격자 밖
    // grid[nx][ny] 처리
}`,
    },

    { h: '유형 2 · 완전탐색 / 백트래킹' },
    {
      p: '가능한 모든 경우를 빠짐없이 시도하는 유형. N이 작을 때(보통 ≤ 20) 쓴다. 순열·조합·부분집합을 재귀로 만들고, 가망 없는 가지는 일찍 잘라낸다(가지치기).',
      terms: [
        { word: '백트래킹', desc: '모든 경우를 재귀로 탐색하되, 조건에 안 맞는 가지는 더 안 가고 되돌아오는(가지치기) 완전탐색.' },
      ],
    },
    {
      code: `// 조합/부분집합 만들기 — 고르거나(O) 안 고르거나(X)
void dfs(int idx, List<Integer> picked) {
    if (idx == n) { check(picked); return; }
    picked.add(a[idx]);
    dfs(idx + 1, picked);          // a[idx]를 고른 경우
    picked.remove(picked.size()-1);
    dfs(idx + 1, picked);          // 안 고른 경우
}`,
    },

    { h: '유형 3 · 그리디 (탐욕법)' },
    {
      p: '매 순간 "지금 가장 좋아 보이는 선택"을 하는 유형. 보통 먼저 정렬한 뒤 앞에서부터 욕심껏 고른다. "이 선택이 정말 최선인가"를 증명/직관으로 확신할 수 있을 때만 쓴다.',
      terms: [
        { word: '그리디', desc: '전체를 안 보고 매 단계 가장 이득인 선택을 반복하는 기법. 정렬과 짝을 이루는 경우가 많다.' },
      ],
    },
    {
      code: `// 회의실 배정류 — 끝나는 시간 기준 정렬 후 욕심껏 선택
Arrays.sort(meet, (a, b) -> a[1] - b[1]);   // 끝나는 시간 오름차순
int end = 0, count = 0;
for (int[] m : meet)
    if (m[0] >= end) { end = m[1]; count++; } // 안 겹치면 선택`,
    },

    { h: '유형 4 · 그래프 탐색 (BFS / DFS)' },
    {
      p: '미로 최단거리, 영역 개수(섬), 연결 관계 문제. 최단거리(칸 단위)는 BFS, 모든 경로·영역 채우기는 DFS. 왼쪽 메뉴의 BFS/DFS 페이지에서 동작을 애니메이션으로 확인하라.',
    },

    {
      callout: {
        tone: 'good',
        icon: '🎯',
        title: '합격 전략',
        body: '낯선 고난도 한 문제에 매달리기보다, 풀 수 있는 유형(구현·완전탐색·BFS/DFS)을 빠르고 정확하게 끝내는 게 합격에 유리하다. 내준 문제를 다 풀 수 있으면 떨어질 일이 거의 없다.',
      },
    },
  ],
}
